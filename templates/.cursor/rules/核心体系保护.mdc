---
description: 核心体系保护名录——这些文件构成工作区的核心能力，任何情况下不得删除、清空或降级
alwaysApply: true
---

## 核心体系保护名录

以下体系和文件是本工作区的核心基础设施。**任何情况下不得删除、清空、覆盖或降级。**

| 体系 | 受保护文件 | 职责 |
|------|-----------|------|
| 记忆闭环 | `memory-protocol.mdc`, `.cursor/MEMORY.md`, `.cursor/memory/` | AI 连续性：每日日记 + 长期记忆 |
| 心跳闭环 | `heartbeat-protocol.mdc`, `.cursor/HEARTBEAT.md` | 自主巡检：定期检查 → 发现问题 → 通知用户 |
| 调度闭环 | `scheduler-protocol.mdc`, `cron-jobs.json` | 定时任务：自然语言创建 → 热加载 → 自动执行 |
| 会话协议 | `soul.mdc`, `agent-identity.mdc`, `.cursor/BOOTSTRAP.md` | 出生仪式 + 每次会话启动读取流程 |
| 身份系统 | `user-context.mdc`, `workspace-rules.mdc` | 用户画像 + 工作区安全边界 |
| 工具能力 | `tools.mdc`, `cursor-capabilities.mdc` | Agent 能力清单 + 决策树 |

## 保护规则

1. 合并外部更新时，必须在合并后检查上述文件全部存在且内容完整
2. 重构或清理时，这些文件不在清理范围内
3. 如果被意外删除，从 Git 历史或备份恢复
