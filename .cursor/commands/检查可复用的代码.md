# 检查可复用代码

扫描整个代码库，找出可以复用的模块和代码，更新至文档。

## 执行步骤

1. 扫描所有模块，识别重复或高度相似的代码段
2. 分析哪些可以提取为公共模块
3. 输出发现清单（位置、相似度、建议提取方式）
4. 更新项目文档中的公共模块索引

## 原则

- 只有"改一处必须同步改另一处"的才算真重复
- 不为复用而复用——简单的工具函数就地定义即可
- 考虑跨模块依赖的管理成本
