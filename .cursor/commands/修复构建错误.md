# 修复构建

构建失败或类型报错时使用。

## 执行步骤

1. 运行构建命令，捕获完整错误输出
2. 分析所有错误，按依赖关系排序（先修被依赖的）
3. 逐个修复，每修一个就重新构建验证
4. 全部修复后做一次完整构建确认

## 原则

- 最小改动原则——只修构建错误，不顺手重构
- 不改接口行为——修类型声明而非改逻辑
- 缺依赖就装依赖，不换方案
- 全部修完后调用"审查刚才的文件与操作"确认没引入新问题
