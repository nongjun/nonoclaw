---
description: 文档生成与维护的统一策略——禁止生成临时性文档，只维护长期有效的知识
alwaysApply: true
---

## 文档铁律

### 禁止生成的文档

以下类型的文档**绝对不要创建**，无论任何场景：

- **变更日志** — Git 是唯一的变更记录器，不要写 `变更日志/xxx.md`
- **巡检报告** — 一次性快照，写完即过期
- **审计报告 / 审查报告** — 同上
- **测试报告** — 同上
- **完成报告 / 验证报告** — 同上
- **经验提取独立文件** — 可复用的经验直接写入 Cursor rules，不要写独立 .md

### 允许保留的文档

仅在 `文档/` 中维护**长期有效**的知识：

| 类型 | 说明 | 更新时机 |
|---|---|---|
| 核心信念 | 团队基线、技术栈标准、编码规范 | 规则变化时修改 |
| 设计文档 | 仅保留仍在指导开发的架构设计 | 架构变化时修改 |
| 参考资料 | API文档、第三方对接、数据库Schema | 接口变化时修改 |
| 产品规格 | 业务规则、判断逻辑、决策标准 | 业务决策变化时修改 |
| 凭据与配置 | 环境变量、密钥、连接信息 | 配置变化时修改 |
| 操作手册 | 操作SOP | 流程变化时修改 |
| 执行计划 | 仅 `进行中/` 和 `技术债务/`，完成即删除 | 任务推进时更新 |

### 文档编写原则

1. **不写代码示例** — 充分信任大模型的理解能力，规则用自然语言描述
2. **Git 管理历史** — 文档只反映当前状态，不内嵌变更记录/日期标注
3. **代码即文档** — 实现细节从代码中读取，文档只记录代码无法表达的信息（决策原因、业务规则、架构约束）
4. **人类决策必须记录** — 判断规则、判定条件、业务逻辑、审核过的决策，这些必须在文档中保留，且决策变更时同步更新文档
5. **执行计划完成即删** — 任务完成后删除计划文件，不归档到"已完成"
