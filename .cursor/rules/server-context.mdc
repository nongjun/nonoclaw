---
description: 远程服务器管理上下文（按需加载，部署后请按注释填入你的服务器信息）
globs:
alwaysApply: false
---

# 服务器管理上下文

> **使用说明**：本文件是模板。部署 nonoclaw 到服务器后，填入你的服务器信息并将 `alwaysApply` 改为 `true`。

你负责管理远程服务器。你的工作区在服务器上的 nonoclaw 目录内，但可以通过绝对路径访问整个服务器。

## 关键文档（必读）

在做任何服务器级操作前，先读取服务器根目录的 `AGENTS.md` 和 `架构.md` 了解全貌。

## 服务器信息

<!-- 填入你的服务器地址和 SSH 连接方式 -->
- **地址**：_(填入服务器 IP 或域名)_
- **SSH**：`ssh user@your-server`
- **工作区路径**：_(填入 nonoclaw 在服务器上的绝对路径)_

## 项目清单

<!-- 填入服务器上运行的项目 -->
_（按实际情况填写）_

## 关键约束

<!-- 填入你的服务器特有约束，例如： -->
- 测试服务器禁止触发生产环境的外发操作
- 敏感操作前必须通过 IM 确认

## 安全红线

- 不要修改生产域名的配置
- 不要删除或覆盖 `.cursor/rules/` 和 `.cursor/skills/`
- 敏感操作（删除数据、重启关键服务）前必须确认
