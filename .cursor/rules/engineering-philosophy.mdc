---
description: 工程哲学与编码标准——项目通用的工程原则、安全底线、编码风格
alwaysApply: true
---

# 工程哲学

## 第一原则

实事求是，大于迎合用户。发现方案有问题就直说，认为用户判断有误就明确指出并给出理由，不为了让用户高兴而隐瞒风险或附和错误决策。宁可当场让用户不舒服，也不让项目埋雷。

## 根因驱动，拒绝防御式补丁

遇到问题时，必须追溯到根本原因并在源头修复，禁止在下游添加防御性检查来掩盖问题。
修复根因后，审视问题暴露的结构缺陷，顺势优化代码结构。

## 一条路径走到底，不做双轨兜底

同一件事只走一条完整路径，不做两条路互相兜底。一条路径的逻辑完整性和可维护性，远大于两条路径带来的"安全感"。双轨意味着双份 bug、双份状态同步、和"到底谁说了算"的歧义。当发现需要兜底时，说明主路径还不够可靠——去加固主路径，而不是再开一条。

## 始终面向正式版编码

不写临时方案、不留 TODO 欠债、不做"先顶一下"的妥协。
每一次修改都按中长期生产标准交付，就像这是最终版本。

---

# 安全底线

## 绝对禁止

- 禁止在代码中硬编码 API Key、密码、Token、密钥
- 禁止在日志中输出密码、密钥等敏感信息
- 禁止将 .env 文件提交到 Git 仓库
- 禁止在生产环境使用调试模式或关闭认证

## 安全检查清单

每次提交代码前，确认：
- 用户输入已做安全校验，防止注入攻击
- API 接口有认证和权限校验
- 错误信息不暴露系统内部细节（堆栈、路径）
- 敏感配置通过环境变量管理
- 发现密钥泄露必须立即轮换

---

# 编码风格

## 优先不可变

默认使用不可变方式处理数据——创建新对象而非修改原对象。
- 函数参数、共享状态传递时，不要直接修改传入的对象
- 并发场景下优先用不可变数据避免竞态

## 文件组织

多个小文件优于少数大文件：
- 高内聚，低耦合
- 典型 200-400 行，最多 800 行
- 按功能/领域组织，而非按类型

## 错误处理

- 在每个层级显式处理错误
- 面向用户的代码提供友好的错误提示
- 服务端记录详细的错误上下文
- 绝不静默吞掉错误

## 输入验证

- 尽可能使用基于 Schema 的验证（类型、格式、范围）
- 快速失败并提供清晰的错误信息
- 绝不信任外部数据（API 响应、用户输入、文件内容）

## 代码整洁

修改代码时直接删除旧代码，禁止用注释方式保留：
- 不要注释掉旧实现再写新实现——直接替换
- 不要保留"以防万一"的备份代码——Git 历史就是备份
- 发现已有的注释废代码，顺手清理

## 质量清单

- [ ] 代码可读性好，命名规范
- [ ] 函数短小（<50 行）
- [ ] 文件聚焦（<800 行）
- [ ] 无深层嵌套（>4 层）
- [ ] 正确的错误处理
- [ ] 无硬编码值（使用常量或配置）
- [ ] 无被注释掉的废弃代码
